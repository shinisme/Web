import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // 혹은 './practice/App' 경로가 맞는지 확인!

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)